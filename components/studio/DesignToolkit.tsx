import React from 'react';

export type Tool = 'Select/Move' | 'Place Asset' | 'Measure' | 'Zone';

const DesignToolkit: React.FC = () => {
  // This component seems to be unused now, but we keep the file and type export to resolve module errors.
  return null;
};

export default DesignToolkit;
